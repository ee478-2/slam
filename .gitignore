# --- ROS / catkin build artifacts ---
build/
devel/
install/
logs/
.catkin_tools/
*.catkin_workspace

# --- Python ---
__pycache__/
*.py[cod]
*.egg-info/
.eggs/

# --- Large / generated data (track sources, not outputs) ---
# NOTE: map deliverables (*.pgm/*.yaml/*.stl/*.world) are tracked on purpose.
*.bag
*.db
*.pyc
*.pt
*.onnx
*.engine
*.torchscript
*.weights
data/storefront_yolo/raw/
data/storefront_yolo/dataset/
training/storefront_yolo/runs/

# 16 MB third-party mesh — exceeds the 10 MB commit rule; re-fetch from the
# realsense_gazebo_plugin upstream when the simulator needs it.
nexus_4wd_mecanum_simulator/realsense_gazebo_plugin/meshes/d435.dae

# --- Editor / OS ---
.vscode/
.idea/
*.swp
*~
.DS_Store
